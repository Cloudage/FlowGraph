@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# ExpressionKit is bundled with FlowGraph - no need to find it separately

# Include the targets
include("${CMAKE_CURRENT_LIST_DIR}/FlowGraphTargets.cmake")

check_required_components(FlowGraph)